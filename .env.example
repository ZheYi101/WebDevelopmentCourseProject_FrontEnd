# Frontend request base path. Keep empty or set to /api in most cases.
VITE_API_BASE_URL=

# Local Vite dev proxy target.
VITE_API_PROXY_TARGET=http://127.0.0.1:8080

# Vercel server-side proxy target. Set this only in Vercel environment variables.
# API_PROXY_TARGET=http://127.0.0.1:8080
